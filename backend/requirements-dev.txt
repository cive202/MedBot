# Development and test dependencies.
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.3.4,<9
pytest-asyncio>=0.25.0,<2
httpx>=0.28.1          # also used by the app; pinned here for the test client
